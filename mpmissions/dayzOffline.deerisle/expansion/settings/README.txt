Current Expansion AI reads patrol settings from this folder.

Expected file:
- AIPatrolSettings.json

With current Expansion AI builds, the server can generate default settings on first startup if the file is missing.
